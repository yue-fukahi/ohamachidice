import { Box, Typography } from "@mui/material";

const Title = () => (
  <Box
    sx={{
      textAlign: "center",
    }}
  >
    <Typography variant="h1">OHAMACHI DICE</Typography>
  </Box>
);

export { Title };
